[defaults]
; inventory = ./inventory/hosts.ini
inventory = ./inventory/playground-hosts.ini
collections_path = ./collections
roles_path = ./roles
# Skip host key checking to avoid prompt (Finger print)
host_key_checking = False